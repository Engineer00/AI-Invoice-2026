# Railway config - uses Dockerfile (bypasses Railpack build plan errors)
# Root Directory: leave empty (repo root)

[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
